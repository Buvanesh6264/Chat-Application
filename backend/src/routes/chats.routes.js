import { Router } from 'express';

const router = Router();

// TODO: GET /, GET /:id/messages, POST /group, POST /:id/members, DELETE /:id/members/:userId — build order step 6

export default router;
