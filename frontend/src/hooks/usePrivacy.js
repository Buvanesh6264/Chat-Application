// TODO: fetch a user's profile via GET /users/:id/profile, which already enforces privacy
// server-side (services/privacy.js) — this hook just surfaces loading/error state to components.
// Build order step 4/5.
export const usePrivacy = () => {
  throw new Error('usePrivacy not implemented yet');
};
